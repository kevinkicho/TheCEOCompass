# CEO Compass

## Quick Start

```bash
# Frontend
cd frontend && npm install && npm run dev

# Agent (requires Firebase service account key in agent/)
cd agent && npm install && node index.js
```

Requires Ollama on `localhost:11434` with `gemma4:latest`.
